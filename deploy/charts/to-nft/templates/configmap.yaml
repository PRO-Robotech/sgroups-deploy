---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "to-nft.fullname" . }}-agent-config
  labels:
    {{- include "to-nft.labels" . | nindent 4 }}
data:
  config.yaml: |
    {{ .Values.agent.config | toYaml | nindent 4 }}
