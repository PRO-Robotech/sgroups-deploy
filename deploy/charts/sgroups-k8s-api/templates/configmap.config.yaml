---
apiVersion: v1
kind: ConfigMap
metadata:
  name: "{{ include "sgroups.fullname" . }}-config"
  labels:
    {{- include "sgroups.labels" . | nindent 4 }}
data:
  config.yaml: |
    {{- toYaml .Values.config | nindent 4 }}
