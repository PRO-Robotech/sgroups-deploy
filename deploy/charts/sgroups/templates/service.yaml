apiVersion: v1
kind: Service
metadata:
  name: {{ include "sgroups.fullname" . }}
  labels:
    {{- include "sgroups.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: grpc
      protocol: TCP
      name: grpc
  selector:
    {{- include "sgroups.selectorLabels" . | nindent 4 }}
