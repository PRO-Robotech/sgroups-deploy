
{{- $dbUrl      := "" }}
{{- $dbPort     := "" }}
{{- $dbName     := "" }}
{{- $dbUsername := "" }}
{{- $dbPassword := "" }}
{{- $extPG := .Values.externalDatabase -}}
{{- $pg := .Values.postgresql -}}

{{- if $extPG.enabled }}
{{- $dbUrl      = $extPG.url }}
{{- $dbPort     = $extPG.port }}
{{- $dbName     = $extPG.database | urlquery }}
{{- $dbUsername = $extPG.username | urlquery }}
{{- $dbPassword = $extPG.password | urlquery }}
{{- else }}
{{- $dbUrl      = printf "%s-postgresql.%s.svc" $.Release.Name $.Release.Namespace }}
{{- $dbPort     = $pg.primary.service.ports.postgresql }}
{{- $dbName     = $pg.auth.database | urlquery }}
{{- $dbUsername = $pg.auth.username | urlquery }}
{{- $dbPassword = $pg.auth.password | urlquery }}
{{ end }}

{{- $connArgs := .Values.databaseConnectionStringArgs | default dict }}
{{- $queryList := list }}

{{- range $key, $value := $connArgs }}
  {{- if $value }}
    {{- $queryList = append $queryList (printf "%s=%s" ($key | toString | urlquery) ($value | toString | urlquery)) }}
  {{- end }}
{{- end }}

{{- $queryString := "" }}
{{- if gt (len $queryList) 0 }}
  {{- $queryString = printf "?%s" (join "&" $queryList) }}
{{- end }}

{{- $connString       := printf "postgres://%s:%s@%s:%s/%s" $dbUsername $dbPassword $dbUrl $dbPort $dbName }}
{{- $connStringGoose  := printf "%s?sslmode=disable" $connString }}
{{- $connStringApp    := printf "%s%s" $connString $queryString }}

---
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "sgroups.fullname" . }}-database
  labels:
    {{- include "sgroups.labels" . | nindent 4 }}
stringData:
  DATABASE_URL:             "{{ $dbUrl }}"
  DATABASE_PORT:            "{{ $dbPort }}"
  DATABASE_NAME:            "{{ $dbName }}"
  DATABASE_USERNAME:        "{{ $dbUsername }}"
  DATABASE_PASSWORD:        "{{ $dbPassword }}"
  SG_GOOSE_POSTGRES_URL:    "{{ $connStringGoose | urlquery }}"
  SG_STORAGE_POSTGRES_URL:  "{{ $connStringApp | urlquery }}"
